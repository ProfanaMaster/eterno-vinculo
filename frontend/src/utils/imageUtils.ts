/**
 * Utilidades para manejar URLs de imágenes
 */

/**
 * Función placeholder para getProxiedImageUrl
 * En el estado actual, simplemente retorna la URL original
 * @param originalUrl URL original de la imagen
 * @returns URL original sin modificaciones
 */
export const getProxiedImageUrl = (originalUrl: string): string => {
  return originalUrl || '';
};




